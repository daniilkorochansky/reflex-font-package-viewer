name: Build FPACK Editor

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            python_arch: x64
          - arch: x86
            python_arch: x86

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          architecture: ${{ matrix.python_arch }}
          cache: pip

      - name: Install dependencies
        shell: cmd
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller==6.11.1

      - name: Build executable
        shell: cmd
        run: |
          python -m PyInstaller ^
            --noconfirm ^
            --clean ^
            --onefile ^
            --windowed ^
            --name "FPACK_Editor" ^
            --icon "FPACK_Editor.ico" ^
            --version-file "version_info.txt" ^
            fpack_editor.py

      - name: Package ZIP
        shell: pwsh
        run: |
          Compress-Archive -Path "dist/FPACK_Editor.exe" -DestinationPath "BXML_FPACK_Tools_FPACK_Editor_${{ matrix.arch }}.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: FPACK_Editor_${{ matrix.arch }}
          path: BXML_FPACK_Tools_FPACK_Editor_${{ matrix.arch }}.zip

      - name: Add release assets
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: BXML_FPACK_Tools_FPACK_Editor_${{ matrix.arch }}.zip
